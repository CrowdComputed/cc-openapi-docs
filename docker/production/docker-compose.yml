services:
  cc-openapi-docs:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    image: cc-openapi-docs
    ports:
      - "3005:3000"
    env_file:
      - ../../.env
